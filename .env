# Runpod / Ollama-compatible proxy base
RUNPOD_BASE_URL=https://u6b9tff7jtf7k0-11434.proxy.runpod.net/

# Endpoints (defaults match Ollama)
RUNPOD_GENERATE_PATH=/api/generate
RUNPOD_CHAT_PATH=/api/chat
RUNPOD_TAGS_PATH=/api/tags

# Default model for general use (fast)
RUNPOD_MODEL=qwen2.5:latest

# Optional: "quality" model for important structured outputs
RUNPOD_MODEL_STRONG=qwen2.5:32b

# Timeouts + retries (tune for demo safety)
RUNPOD_TIMEOUT=45
RUNPOD_RETRY_TOTAL=2
RUNPOD_RETRY_BACKOFF=1

# Demo safety mode (we’ll implement this later)
DEMO_MODE=true
